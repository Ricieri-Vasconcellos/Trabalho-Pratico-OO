try {
            ClienteStandard cliente = new ClienteStandard("Roberto", "12334");

            Comida labs = new Comida("C001", "Bread", 5.00, 10, 2, false);
            Pedido p = new Pedido(cliente);

            System.out.println("Quantidade de estoque: " + labs.getQuantidadeEstoque());

            p.adicionarItem(labs);

            String resposta = "";
            resposta += p.getCliente().getNome() + "\n";
            resposta += "Pedido numero: " + p.getNumeroPedido() + "\n";
            resposta += "Quantidade: " + p.getItens().getFirst().getQuantidade() + "\n";
            resposta += "Nome do produto: " + p.getItens().getFirst().getProduto().getNome() + "\n";
            resposta += "Valor total: " + p.getValorTotal() + "\n";

            p.finalizarVenda(false);

			
            System.out.println(resposta);
			System.out.println("Quantidade restante do produto: " + labs.getQuantidadeEstoque());
        } catch (EstoqueInsuficienteException e) {
            // TODO Auto-generated catch block
            e.printStackTrace();
        } catch (PontosInsuficientesException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}